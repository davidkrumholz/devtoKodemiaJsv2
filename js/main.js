import { createHeader } from "./components/dom.js";
let header = document.getElementById("header");
header.append(createHeader());

// let card = createPostCard();

// document.getElementById("wrapperPosts").append(card);
